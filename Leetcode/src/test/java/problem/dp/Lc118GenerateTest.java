package problem.dp;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class Lc118GenerateTest {

    @Test
    void generate() {
        assertNotNull(new Lc118Generate().generate(5));
    }
}